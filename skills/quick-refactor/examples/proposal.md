# Choices-stage example

[proposal.json](proposal.json) illustrates the initial comparison stage using [the authoring contract](../authoring.md). No selection or detailed design is included. Retry uses a compact flow; the other simple candidates omit diagrams. After a user selects, add the [selected-design fields](../design.md) in a new artifact.

The example is illustrative and has no real source evidence. HTML and Markdown are derived from JSON; evaluation audit material stays separate.
