# Luna report worker

Read the supplied `handoff.md` and [renderer contract](scripts/README.md). Work only from the handoff and renderer assets. Preserve the recommendation, technical statements, citations, evidence strengths, uncertainty, and executed/proposed check distinction. Return missing technical information to Sol as a concise question; resume after its correction.

1. Map the handoff into `proposal.json` using the existing schema and template. Lead with recommendation and paired process flowcharts as required by the renderer; add a short choices table after process. Include all supported choices and the reason for a shorter list. Keep optional sections concise.
2. Produce `before.mmd` and `after.mmd` from Sol's flow description. Show the responsibility moving and invariant preserved. Add another diagram pair only if Sol identifies a distinct lifecycle or ordering question. Reuse the template styling and existing renderer; author content and diagrams without redesigning assets.
3. Run `python3 /absolute/skill/path/scripts/render_proposal.py /absolute/output/path/proposal.json -o /absolute/output/path/report.html`. This compiles Mermaid with headless Chrome and validates the artifact. Fix schema or Mermaid syntax errors locally; send technical uncertainties to Sol. On an environment or permission failure, report the exact failure instead of changing the design or downloading dependencies.
4. Complete a fidelity preflight before returning to Sol: compare all choices, recommendation, source references, confidence qualifiers, preserved invariants, and first-PR constraints with the handoff. Use the renderer's allowed `strength` values; retain additional qualifiers such as “strong for existing behavior, moderate for the proposed design” in evidence text. Convert Markdown citations to actual HTML anchors in `body_html`, preserving absolute source paths and line numbers; the renderer escapes the plain-text `evidence.source` field, so place clickable links in `body_html`. Check that these anchors and qualifiers survive in the generated HTML. Inspect visually only when a preview was requested and is available; otherwise state that visual readability was not checked.
5. Return the handoff, JSON and authored diagram paths plus a concise render/fidelity summary for Sol's content review. Sol reviews these authored inputs; the generated HTML, CSS, and SVG stay out of its context. Include unresolved issues and model/effort and usage records when available. Keep generated HTML out of tool output and chat.
